import { Container } from '@/components/generic/Container/Container';
import React from 'react';

export const Contacts: React.FC = async () => {
  return (
    <Container>
      <div>Contacts</div>
    </Container>
  );
};
